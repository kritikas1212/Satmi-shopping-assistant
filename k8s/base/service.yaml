apiVersion: v1
kind: Service
metadata:
  name: satmi-app
  labels:
    app.kubernetes.io/name: satmi-chatbot
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: satmi-chatbot
    app.kubernetes.io/component: api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
